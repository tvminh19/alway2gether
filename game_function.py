# since Game is a high level class, but low level classes still need to use its function,
# this class is to provide the low level classes the functions that they need
class GameFunctions:
    function = {}
